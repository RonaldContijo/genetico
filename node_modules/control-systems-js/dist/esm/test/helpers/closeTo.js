export const closeTo = (expected, precision = 2) => ({
    asymmetricMatch: (actual) => Math.abs(expected - actual) < Math.pow(10, -precision) / 2,
});
//# sourceMappingURL=../../../../src/test/helpers/closeTo.js.map