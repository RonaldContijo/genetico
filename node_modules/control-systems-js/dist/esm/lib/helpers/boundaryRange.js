export const boundaryRange = (start, end, step = 1) => {
    const rangeValues = [];
    for (let i = start; i < end; i += step) {
        rangeValues.push(i);
    }
    return rangeValues;
};
//# sourceMappingURL=../../../../src/lib/helpers/boundaryRange.js.map