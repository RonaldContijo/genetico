const factorialMemo = [1];
const calculateFactorial = (number) => {
    let result = 1;
    for (let i = 1; i <= number; i++) {
        result *= i;
        factorialMemo[i] = result;
    }
    return result;
};
const memo = (fn, num) => {
    return factorialMemo[num] || fn(num);
};
/**
 * Calculates the factorial of a number
 * @param num number to have the factorial calculated
 * @returns calculated value
 */
export const factorial = (num) => memo(calculateFactorial, num);
//# sourceMappingURL=../../../../src/lib/helpers/factorial.js.map