import { Complex } from '../complex';
import { IFrequencyRange } from './frequencyRange.entities';
export declare class FrequencyRange implements IFrequencyRange {
    getDefault(poles: Complex[], zeros: Complex[], rangeFactor?: number, numOfSamples?: number): number[];
}
