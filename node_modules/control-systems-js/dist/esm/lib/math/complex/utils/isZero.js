/**
 * Check if the real and imaginary part of a number is zero
 * @param complex
 * @returns
 */
export const isZero = (complex) => {
    return complex.re === 0 && complex.im === 0;
};
//# sourceMappingURL=../../../../../../src/lib/math/complex/utils/isZero.js.map