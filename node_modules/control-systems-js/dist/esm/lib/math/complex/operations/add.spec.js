import { describe, expect, it } from 'vitest';
import { add } from './add';
describe('add function', () => {
    // Complex + Complex
    it('Should be able to add two complex numbers', () => {
        const a = { re: 1, im: 2 };
        const b = { re: 3, im: 4 };
        const result = { re: 4, im: 6 };
        expect(add(a, b)).toEqual(result);
    });
    // Complex + Number
    it('Should be able to add a complex number and a number', () => {
        const a = { re: 1, im: 2 };
        const b = 3;
        const result = { re: 4, im: 2 };
        expect(add(a, b)).toEqual(result);
    });
    // Number + Complex
    it('Should be able to add a number and a complex number', () => {
        const a = 3;
        const b = { re: 1, im: 2 };
        const result = { re: 4, im: 2 };
        expect(add(a, b)).toEqual(result);
    });
    // Number + Number
    it('Should be able to add two numbers', () => {
        const a = 1;
        const b = 2;
        const result = 3;
        expect(add(a, b)).toEqual(result);
    });
    // Invalid input
    it('Should throw an error if given invalid input', () => {
        const a = 'a';
        const b = 'b';
        expect(() => add(a, b)).toThrow('Wrong type values for operation a: a, b: b');
    });
});
//# sourceMappingURL=../../../../../../src/lib/math/complex/operations/add.spec.js.map