import { Complex } from '../complex';
/**
 * Calculates the power of a number, given an exponent (base)^exponent
 * @param base
 * @param exponent
 */
export declare function pow(base: Complex, exponent: Complex): Complex;
export declare function pow(base: number, exponent: Complex): Complex;
export declare function pow(base: Complex, exponent: number): Complex;
export declare function pow(base: number, exponent: number): number;
