"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const pow_1 = require("./pow");
(0, vitest_1.describe)('pow', () => {
    const EPSILON = 1e-10;
    (0, vitest_1.it)('should be able to calculate the power of a real number with positive real exponent', () => {
        const base = { re: 2, im: 0 };
        const exponent = { re: 2, im: 0 };
        const result = (0, pow_1.pow)(base, exponent);
        const expected = { re: 4, im: 0 };
        (0, vitest_1.expect)(result.re).toBeCloseTo(expected.re, EPSILON);
        (0, vitest_1.expect)(result.im).toBeCloseTo(expected.im, EPSILON);
    });
    (0, vitest_1.it)('should be able to calculate the power of a real number with negative real exponent', () => {
        const base = { re: 2, im: 0 };
        const exponent = { re: -2, im: 0 };
        const result = (0, pow_1.pow)(base, exponent);
        const expected = { re: 0.25, im: 0 };
        (0, vitest_1.expect)(result.re).toBeCloseTo(expected.re, EPSILON);
        (0, vitest_1.expect)(result.im).toBeCloseTo(expected.im, EPSILON);
    });
    (0, vitest_1.it)('should be able to calculate the power of a complex number with a positive real exponent', () => {
        const base = { re: 2, im: 1 };
        const exponent = { re: 2, im: 0 };
        const result = (0, pow_1.pow)(base, exponent);
        const expected = { re: 3, im: 4 };
        (0, vitest_1.expect)(result.re).toBeCloseTo(expected.re, EPSILON);
        (0, vitest_1.expect)(result.im).toBeCloseTo(expected.im, EPSILON);
    });
    (0, vitest_1.it)('should be able to calculate the power of a complex number with a negative real exponent', () => {
        const base = { re: 2, im: 1 };
        const exponent = { re: -2, im: 0 };
        const result = (0, pow_1.pow)(base, exponent);
        const expected = { re: 0.0909090909, im: -0.1212121212 };
        (0, vitest_1.expect)(result.re).toBeCloseTo(expected.re, EPSILON);
        (0, vitest_1.expect)(result.im).toBeCloseTo(expected.im, EPSILON);
    });
    (0, vitest_1.it)('should be able to calculate the power of a complex number with a complex exponent', () => {
        const base = { re: 2, im: 1 };
        const exponent = { re: 2, im: 1 };
        const result = (0, pow_1.pow)(base, exponent);
        const expected = { re: -0.5048, im: 3.1041 };
        (0, vitest_1.expect)(result.re).toBeCloseTo(expected.re, EPSILON);
        (0, vitest_1.expect)(result.im).toBeCloseTo(expected.im, EPSILON);
    });
});
//# sourceMappingURL=../../../../../../src/lib/math/complex/operations/pow.spec.js.map