"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.abs = void 0;
const isComplex_1 = require("../utils/isComplex");
/**
 * Calculates the absolute value of a number or complex
 * For complex it evaluates the sqrt of re^2 + im^2
 * @param value
 * @returns
 */
const abs = (value) => {
    if ((0, isComplex_1.isComplex)(value)) {
        const sumOfQuadratic = Math.pow(value.re, 2) + Math.pow(value.im, 2);
        const result = Math.sqrt(sumOfQuadratic);
        return result;
    }
    return Math.abs(value);
};
exports.abs = abs;
//# sourceMappingURL=../../../../../../src/lib/math/complex/operations/abs.js.map