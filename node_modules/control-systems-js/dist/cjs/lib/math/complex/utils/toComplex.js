"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toComplex = void 0;
const complex_1 = require("../complex");
const isNumber_1 = require("./isNumber");
/**
 * Transform a real number or an array of numbers into a Complex one
 * @param value
 * @returns Complex or Array of Complex
 */
const toComplex = (value) => {
    if ((0, isNumber_1.isNumber)(value)) {
        return (0, complex_1.complex)(value, 0);
    }
    if (Array.isArray(value)) {
        return value.map((v) => (0, complex_1.complex)(v, 0));
    }
    throw new Error(`Invalid types: ${value}`);
};
exports.toComplex = toComplex;
//# sourceMappingURL=../../../../../../src/lib/math/complex/utils/toComplex.js.map